# MCP (Model Context Protocol) Configuration Example

# Enable MCP Features
ENABLE_MCP=true

# Your OpenAI API Key (required)
OPENAI_API_KEY=your-api-key-here

# OpenAI Model (must support function calling: gpt-4, gpt-3.5-turbo, etc.)
OPENAI_API_MODEL=gpt-3.5-turbo-16k

# OpenAI API Configuration
OPENAI_API_BASE_URL=https://api.openai.com
OPENAI_API_TEMPERATURE=1.0

# MCP Servers Configuration (JSON format)
# {{PROJECT_ROOT}} will be automatically replaced with your project directory
# This makes the configuration portable across different machines!
MCP_SERVERS={"filesystem":{"command":"node","args":["mcp-filesystem-server.mjs"],"env":{"MCP_FS_BASE_PATH":"{{PROJECT_ROOT}}"}}}

# To allow access to parent directory (e.g., entire Desktop):
# MCP_SERVERS={"filesystem":{"command":"node","args":["mcp-filesystem-server.mjs"],"env":{"MCP_FS_BASE_PATH":"{{PROJECT_ROOT}}/.."}}}

# To specify a custom path:
# MCP_SERVERS={"filesystem":{"command":"node","args":["mcp-filesystem-server.mjs"],"env":{"MCP_FS_BASE_PATH":"/path/to/directory"}}}

# Optional: Site Password
# SITE_PASSWORD=
